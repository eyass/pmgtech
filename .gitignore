node_modules/
.next/
out/
build/
.DS_Store

# Environment — never commit real credentials
.env
.env*.local
!.env.example

.vercel
*.tsbuildinfo
next-env.d.ts

npm-debug.log*
yarn-error.log*
.eslintcache

# Agent worktrees. Transient full-repo checkouts created per subagent — never
# commit them: they are working copies, not source, and they are locked while in use.
.claude/worktrees/
